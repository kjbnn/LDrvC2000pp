Логирование
Log -> Файл + sl_log.Add -> FormTimer -> memo1 + sl_log.Delete

Самодиагностика
Live: array.  (main, pps, line1, line2, ...)
Каждый живой зануляет свой элемент массива

ser.RaiseExcept := False не вызывает Exception порта